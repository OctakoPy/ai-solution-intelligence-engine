{
  "description": "Monorepo root for the Solution Intelligence Engine (Python backend + Vite/React frontend).",
  "engines": {
    "node": ">=20.0.0"
  },
  "name": "solution-intelligence-engine",
  "private": true,
  "scripts": {
    "web": "npm run dev --workspace apps/web",
    "web:build": "npm run build --workspace apps/web",
    "web:e2e": "npm run e2e --workspace apps/web",
    "web:lint": "npm run lint --workspace apps/web",
    "web:test": "npm run test --workspace apps/web",
    "web:typecheck": "npm run typecheck --workspace apps/web"
  },
  "version": "0.10.0",
  "workspaces": [
    "apps/web"
  ]
}
